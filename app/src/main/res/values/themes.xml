<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Oficialapp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/verde</item>
        <item name="colorPrimaryVariant">@color/verde</item>
        <item name="android:statusBarColor">@color/verde</item>
    </style>

    <style name="Theme.Oficialapp" parent="Base.Theme.Oficialapp" />
</resources>